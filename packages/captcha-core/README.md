# @whoize/captcha-core

Framework-agnostic types, presets, configuration normalization, shape masks,
and geometry helpers used by the WHOIZE motion CAPTCHA research prototype.

This package deliberately contains no browser UI, persistence, authentication,
or production anti-abuse policy.
